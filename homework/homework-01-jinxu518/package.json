{
  "name": "homework-01-jinxu518",
  "version": "1.0.0",
  "description": "1. Make a new directory\r 2. Open the terminal on VS Code and cd to that directory\r 3. Initialize a new NodeJS project by the command: npm init -y\r 4. Install ExpressJS: npm i express --save\r 5. Install nodemon: npm i nodemon --save\r 6. Add the line to 'scripts' of the file package.json\r \"start\": \"nodemon app.js\"\r 7. Create the file app.js\r 8. Run the app: npm start\r ## Implement the following Restful APIs. You can keep the data in an array.\r 1. POST /products: To add a new product. The body of this request should contain the product's information. You should attach the header \"authorization\" with the scheme \"Bearer <token>\" (this token is returned when you log in successfully). If successful, the body return {success: true, data: <new Product>}. Otherwise, it return {success: false, error: <message>}\r 2. PUT /products/{id}: To add a new Product. The body of this request should contain the product's information. You should attach the header \"authorization\" with the scheme \"Bearer <token>\" (this token is returned when you log in successfully). If successful, the body return {success: true}. Otherwise, it return {success: false, error: <message>}\r 3. DELETE /products/{id}: To add a new product. You should attach the header \"authorization\" with the scheme \"Bearer <token>\" (this token is returned when you log in successfully). If successful, the body return {success: true}. Otherwise, it return {success: false, error: <message>}\r 4. GET /products: To get all products. You should attach the header \"authorization\" with the scheme \"Bearer <token>\" (this token is returned when you log in successfully). If successful, the body return {success: true, data: <list of products>}. Otherwise, it return {success: false, error: <message>}\r **Note: Please submit the homework by `10:00 PM CST`**",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
